# Codex OAuth 剩余手动验收

以下项目需要账号持有人在浏览器中完成登录/授权，因此未由自动化代替。其余配置显隐、VSIX 安装、Codex app-server 启动和未登录状态读取均已验证。

## 1. 登录与模型列表

1. 在 VS Code 的“AI 做题陪练”侧栏展开“AI 接口配置”。
2. 将“兼容模式”设为“OpenAI 官方”，“OpenAI 认证”设为“Codex OAuth”。
3. 点击“浏览器登录”；在浏览器中自行完成账号登录和授权。
4. 回到 VS Code，确认状态显示已登录，且“提示/评分模型”和“补全模型”是两个独立下拉框。
5. 确认模型来自当前账号实际返回的列表；若账号未返回 Spark，界面不得伪造 Spark。
6. 分别选择教学模型和补全模型，点击“保存 AI 配置”。

也可额外检查“设备码登录”：点击后应显示设备码和可打开的授权入口，取消后界面应恢复为未登录状态。

## 2. 教学调用与 Ghost Text

1. 打开任意本地代码文件和一道题目。
2. 在侧栏触发一次“简单提示”，确认返回教学文本，且没有工具执行或写文件授权请求。
3. 在编辑器中输入一段未完成代码，执行命令“AI 做题陪练：触发一次 AI 补全”。
4. 确认编辑器出现灰色 Ghost Text，并且所用模型是“补全模型”，不是“提示/评分模型”。
5. 确认补全请求不包含题解、标准答案、教师包或教练对话历史。

## 3. 退出登录

完成上述验收后，如需检查退出流程，点击“退出登录”并确认状态恢复为未登录。退出会清除本扩展隔离目录中的 Codex 登录状态；不要在仍需继续测试该账号时提前执行。
